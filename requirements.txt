# Core dependencies with pinned versions for Python 3.12 compatibility
# These versions are tested and known to work together

# NumPy - must be < 2.0 for pandas-ta-classic
numpy==1.26.4

# Pandas - pin to version compatible with numpy 1.26.4 and pandas-ta-classic
pandas==2.2.2

# Market data
yfinance>=0.2.32

# Technical analysis - use pandas-ta-classic (community fork that works on Python 3.12)
pandas-ta-classic==0.3.14b0

# AI integration
google-generativeai>=0.8.3

# Utilities for data fetching
lxml>=4.9.0
html5lib>=1.1
beautifulsoup4>=4.12.0
requests>=2.31.0
